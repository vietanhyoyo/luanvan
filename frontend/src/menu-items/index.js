import dashboard from './dashboard';
import accounts from './accounts';
import schedule from './schedule';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, accounts, schedule]
};

export default menuItems;
